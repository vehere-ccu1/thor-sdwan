I have alread api and I want to develop gui for it and I want to use react for this.
setup the gui for the react keeping in mind that:
1. All the source code should be in src folder of the gui directory.
2. create a theme.js/ts file which will keep all the GUI themes like font, font-color,
 type-of-button, main-page-header back ground, there will be a side menu items (right side slide panel) and a main-page-header, it will starts with login page.
3. Name of the product/company is also saved in the theme file.  Give the product name = "SD-WAN-CMS" and "R & D".
4. Any icons used in the GUI should be of same color mentioned in the theme.ts/js file.
5. The Right-Slide-Main-Manu items would be:
    a. Home
    b. Account
        i. Profile
        ii. Organizations
        iii. Billing
        iv. Access Key
    c. Users
    d. Inventory
        i. Devices
        ii. Tunnels
        iii. Peers
        iv. Tokens
        v. Path Labels
        vi. Traffic App Identification
    e. Traffic Optimization
        i. Path Selections
    f. Security
        i. Firewall
    g. Dhashboards
        i. Network
        ii. Traffic
    h. Troubleshoot
        i. Job
        ii. Notification
    i. About

make the Right-Slide-Main-Manu to left-Slide-Menu


Acount Profile
    There should be option to add/update/delete records and each record must have the fields:
    1. Company Name (Text)
    2. Country (Text Drop-Down List All a2-country-code with the country flag icon)
    3. Notifications (boolean)
    Every record can be shown either grid-view or label/ticket view.
     Use a button to toggle from Grid-View to Ticket-View and vice versa.

 Acount Organizations
     There should be option to add/update/delete records and each record must have the fields:
     1. Name
     2. Group
     3. Tunnel Key Exchange Method
     4. Action [Set-Default]
     Every record can be shown either grid-view or label/ticket view.
     Use a button to toggle from Grid-View to Ticket-View and vice versa.
    (Try to use global look-and-feel for the Data-Grids and the ticket-view)

Users
    There should be option to add/update/delete records and each record must have the fields:
    1. Name
    2. Email
    3. Permission To (Account/Organization)
    4. Entity
    5. Role (Owner/Manager)
    6. Action (Enable/Reset-Password/)
    (Try to use global look-and-feel for the Data-Grids and the ticket-view)

Make all the "Add" (Create new) pannel as Collapse/Expand with "+" option in all the pages.

Add Dark-Mode & Light-Mode option in the Main-Menu. Every page and modules (created or to be created) must follow the modes.

Do not use APPEARANCE Use on one Item and let it be toggle from "Dark-Mode" to "Light-Mode" vise versa.

CLicking on Dark-Mode nothing is hapenning, Expand the SUB-MENU a littile right position from the MAIN-MENUs


Inventory - Traffic App Identification
    Manual App Identifiers
        Create Button -> New App Identification
        Data-Grid
        1. Name
        2. Description
        3. Category
        4. Service Class
        5. Importance
        6. Action [Update/Delete]

    Predefine- App Identifieres in Data-Grid.    
        Data-Grid
        1. Name
        2. Description
        3. Category
        4. Service Class
        5. Importance
        6. Action [Update/Refresh]

Inventory-Devices
    There will be a TAB-Pannel with following tabs
    1. General
    2. Interfaces
    3. DHCP
    4. Routing
    5. Policies
    6. Static Routes
    7. Apps
    8. Logs
    9. Packet Traces
    10. Configuration
    11. Command
Latter I will tell about each tabs    

Rename the Menu-button Security -> Firewall to Security -> Organization Firewall Policies


Security -> Organization Firewall Policies
Header: Firewall Policy with a prominent “Save & Update Devices” button & Search Bard used in "Inventory – Tunnels"

Outbound rules table with columns:
Name
Destination
Source (all set to * Any)
Action (all set to Deny, shown with a red blocked icon)
Description (empty)
Rule Actions (icons for edit, enable/disable, move up/down, delete)

Inbound rules section: A separate table labeled Inbound rules, but it displays:
Name
Destination
Source (all set to * Any)
Action (all set to Deny, shown with a red blocked icon)
Description (empty)
Rule Actions (icons for edit, enable/disable, move up/down, delete)

Just Beside "Outbound rules" and "Inbound rules" Lables add a Create (+) button which will expand a panel for new entry
The Panel will contains following controlls:
1. Enabled
2. Description
3. App/Destination
4. Source
5. Multi IP Address Range (By clicking+ another entry option should be there)
6. Protocols
7. Multi Port Ranges(By clicking+ another entry option should be there)
8. Interfaces
9. Action (Allow/Deny)

Can you please rearrange the "Outbound rules" and "Inbound rules" add-new Panel's controls,
 it is taking too much page size (height) use multiple controls in same line.

Also the + button should become - if we click on this and vice versa. 
Where is the Grid/Ticket view option? It should be there in every page where Data-Grid is used.

Inventory-Devices-Commands Panel should have:
    Command Name (Text)
    Command (Text Area)
    Output (Text Area with none-editable and copy option)
    Button (Run Command)
    Improve Inventory-Devices-Commands, increase the width of the Text-Area. Use copy icon inside text-area at right-top.

Inventory-Devices-General Panel should have:
    Device Name (Text)
    Description (Text Area)
    Approves (boolean)
    Host Name (Text Field, None Editable)
    Serial Number (Text Field, None Editable)
    Machine ID (Text Field, None Editable)    
    Devive Version (Text Field, None Editable)
    GIS MAP Widget to show the device location

Inventory-Devices-Packet-Trace Panel should have:
    Total Packets (Text Field allow between 5 - 65k)
    Capture Duration: (Text Field (Allow 5 sec to 10 seconds))
    Trace (Button) [All three should be in same line]
    A Text-Search Field to search keywords in the Text-Area
    A Readonly Text-Area which will contains the dissected packet informations once the Trace button is clicked.

Use Seperate jsx file for every tab panels in the  Inventory-Devices.

I want to use ClickHouse as Database to store all the configuration localy, 
1. User-Managemnt/ User rolls
2. Audit Trail
3. VPN and Decice Management/Settings


So, create a db folder where you need to manage a sql file by which anyone can create all the tables and views.
Check whether the ClickHouse DB installed locally or not.

Further I will share the details about the modules so that you can create the table along with api (Must support multithread, can handle multiple records same time)

Create a vdev environment, as I will prefer to use python for agent and api.

The word "SD-WAN-CMS" must be saved inside api, create a folder api/resource and inside the api/reoaurce/config.json (Where Product and Company Name will be stored) and images where all the images like logo, icons will be stored.
Set the company name "R & D", and in main-page-header just shift the product name to the left just after the menu button.

Make sure any library you are going to use in api, agent and gui must be safe and free for commercial purpose.



Update the User-Page, and the API to handle following, use ClickHouse to save all the thinks.
Account and Users Management
--------------------------------
Terminology
To start working with SD-WAN-CMS, a user (identified by his e-mail address) should create an account and organization/s.
 The system allows multi-tenancy and defines Accounts and Organizations as described further in this page.
 The email used for creating the Account will be the owner (you may add more users as owners) and billing email of the account.
 You may consider using an email alias for the opening of the Account.

Account
----------
An entity that allows to manage multiple separate networks. A network that includes flexiEdge devices,
 tunnels and other configurations is associated with an Organization. 
 A User in an Account can create multiple Organizations that belong to that Account. 
 An example of an Account is a Manage Service Provider (MSP) or s System Integrator (SI) that manages multiple networks for different customers.

The Account is the billing entity. All Organizations of the Account will be billed in the Account level and through the payment method of the Account.

Group
---------
Group of multiple networks / organizations.
 Used to assign management permission as a group.
 Every organization is associated with one group.

Organization
----------------
An entity that includes all the network inventory and configuration,
 usually a single enterprise with a single network.
 It is possible to connect tunnels between Devices of the same Organization but different Organizations (even under the same account) are not connected,
 hence, it is not possible to create a Tunnel between a Device in Organization A to a Device in Organization B.
 All the network management is done in the scope of an Organization and therefore, it is required to create an organization to manage a network.

Roles and Permissions
===========================
A user in the system is granted access to a resource (Account, Group, or Organization) with permissions according to his role. The permissions are derived by three parameters:

Permission To
----------------
Defines which type of resource is permitted: Account, Organization, or Group

Entity
----------------
The name of the account, organization or group

Role
------------
Defines the permissions allowed for every operation on the resource: Owner, Manager, or Viewer
Based on the permission level, a user is allowed to perform various operations on the resource.
 The user that creates an account becomes the account owner and can invite other users to access resources on his account with different permission levels.
When permitting access to an account, an owner, manager, or viewer roles could be set. Each role grants different permission levels as follows:

Account Roles:
Owner:
Update the account
Handle billing aspects
Create/Update Organizations
Add/Update/Delete users to the account or organizations
Change user permissions
Manage all organizations

Manager:
Create/update organizations
Add/Update/Delete users to the account (only to permissions below the Manager) or organizations
Change user permissions
Manage all organizations

Viewer:
View all organizations

Organization or Group Roles:
Manager:
Manage organization
Add/Update/Delete users to the organization
Change user permissions

Viewer:
See devices and network status
See network reports



Make sure the api is totaly thread-safe.


Device registration
The purpuse of device registration is to connect a newly installed device (using agent at sites) to your CMS-CUI account.

Token creation
To register a device, begin by creating your organization’s token.
 The organization token requires generation only once and is applicable to all devices within a single organization.
 It’s worth noting that having multiple tokens within a single organization is also supported,
 enhancing security measures in the event of unauthorized physical access to a device.
 To create a token from flexiManage, go to Inventory > Tokens and select the “New Token” button.

The token must be a base64 of compressed json file which has following information:
1. CMS API-LIstening public IP and PORT
2. Encryption-key information for communication between agent and the CMS-api (PQC).
3. Organization name
4. random-hex-string, used as token in rest api for validation.
   In every request agent and the api will send Unix-Time-Stamp, SHA256 (random-hex-string + Unix-Time-Stamp).
   and the listener will recalculate the SHA256(random-hex-string+ received-unix-timestamp) and only response if the calculated SHA256==SHA256 (received)

API prefix for the:
1. API should be sdwan_cms_api
2. Agent should be sdwan_agent_api


Device Configuration
 devices can be configured and centrally managed from CMS-GUI.

Device settings
Each device settings can be accessed by clicking on a device name from Inventory > Devices. If the device has been just added or not yet approved, vRouter will not be in running state.

Interfaces settings will open by default, showing current networking configuration.

If the device was approved but not yet configured, all interfaces are shown grayed out as they are not assigned yet.
 To configure an interface it first must be assigned.. Some interfaces may show IP’s since initial configuration was done using local Firewall-Router.

Menu structure
The device settings in CMS-GUI are categorized into the following menus:
General
Configuration
Status
Troubleshooting

Let’s explore each of these menus in detail:

General
The “General” menu retains its core functionality while introducing new features. Here’s what you’ll find:
Hardware Configuration Settings: This section allows you to configure hardware-specific settings for your Firewall-Router (Where agent is installed) device.
IKEv2 Key/Cert Generation: Access buttons to generate IKEv2 keys and certificates, simplifying the process of securing your network.

Configuration
The “Configuration” menu provides a comprehensive set of options for configuring your Firewall-Router (Where agent is installed) device. It includes the following sub-sections:
Interfaces: Configure network interfaces and their settings.
Firewall & NAT: Define firewall rules and Network Address Translation (NAT) settings to control traffic flow.
Static Routes: Configure static routes for precise control over how traffic is routed.
OSPF (Open Shortest Path First): Manage OSPF routing protocols for dynamic network routing.
BGP (Border Gateway Protocol): Configure BGP settings to manage routing between autonomous systems.
Routing Filters: Set up routing filters to control the flow of traffic based on specific criteria.

Status
The “Status” menu provides real-time insights into the health and performance of your Firewall-Router (Where agent is installed) device. It includes the following sections:
Apps: View the status of installed applications.
BGP Neighbors: Monitor the status of BGP neighbors and their connectivity.
DHCP: Check the status of Dynamic Host Configuration Protocol (DHCP) services.
Policies: View and manage network policies.
Routing Table: Examine the current routing table and routing information.
Statistics: Access network statistics to analyze traffic patterns.
VRRP (Virtual Router Redundancy Protocol): Monitor VRRP status for high availability configurations.

Troubleshooting
The “Troubleshooting” menu offers essential tools for diagnosing and resolving issues on your Firewall-Router (Where agent is installed) device. It includes the following sections:
Command Tab: Access a command-line interface for advanced troubleshooting and configuration.
Edge Settings: Configure edge settings for optimal network performance.
Logs: View system logs to track events and identify potential problems.
Packet Traces: Perform packet tracing to analyze network traffic.
Recovery Info: Access recovery information and options to restore normal operation in case of failures or issues.
Proceed to the next section, coverign networking configuration.



Network Configuration
Interfaces page shows columns through which networking can be configured. The following columns are shown by default:
Type - Marks interface as WAN, LAN or TRUNK. Firewall-Router (Where agent is installed) requires at least one WAN and LAN for operation. Multiple WAN and LAN interfaces are supported. TRUNK is used for VLAN operation.
Assigned - When set to “Yes” interface can be configured. Select “No” to not use the interface with Firewall-Router (Where agent is installed).
IPv4 - Shows interface IP
GW - Shows upstream gateway
Metric - Each interface has its own metric. Use metrics to set interface priority (primary, secondary etc.)
Public IP - Automatically detected external IP using STUN. If your CMS-API device is behind a NAT or a router, the public IP column will show the external IP.
Path Labels - Assign Path Lables to logically label or filter traffic. See Path Labels section for more details.
Routing - LAN interfaces support OSPF and BGP. By default OSPF is enabled on LAN
More columns can be added (or removed) by clicking on Modify columns icon in top left corner of the table. Other available columns:
MAC - Displays interface MAC address
MTU - Shows interface MTU size
DHCP/Static - Used with WAN interfaces, WAN can be configured with static IP or set to receive an IP via DHCP.
QoS - Used for setting ISP throughput / speeds so QoS can properly schedule traffic.

Configuration
Network interfaces can be configured from the interfaces table or each interface directly through its settings section.
 Table offers networking information and configuration at a glance while each interface settings offers configuration in greater detail.
 To configure an interface using table, simply switch the Assigned column item to Yes from No. This will allow interface to be configured and used with Firewall-Router.
Once the interface is assigned, next important step is set interface as WAN, LAN or TRUNK port. For vRouter to start, WAN and LAN interfaces are required.
 TRUNK port is relevant for VLAN’s, read more below in separate section on VLAN’s. Multiple WAN and LAN interfaces are supported. 
When assigning a WAN or LAN interface, click on IPv4 column to configure an IP. Remember to use CIDR format.
In addition to table view, each interface can be configured in greater detail by simply clicking on settings icon next to interface name.
Interface settings offer complete and advanced options, starting with settings for WAN interfaces:

Assigned - When set to “Yes” interface can be configured. Select “No” to not use the interface with Firewall-Router.
Type - Marks interface as WAN.
IP Address - Displays / sets IP address of WAN interface. If DHCP is set under “DHCP/static” then IP will be received via upstream.
Gateway - Displays upstream gateway which Firewall-Router is using.
MAC - Displays MAC address of the interface.
MTU - Shows interface MTU size, default 1500 for all interfaces.
Get DNS via DHCP - When enabled, WAN will use DNS received by upstream. When not selected allow setting DNS servers.
Path Labels - Used for granular tunnel creation and Path Selection policies (application based routing).
QoS Policy - Allows selecting QoS policy for the particular WAN interface. Learn more about QoS in its own documentation section.
Bandwidth Tx / Rx - Set upload / download throughput.
Public IP - Displays detected public IP.
STUN - When enabled, allows detection of public IP and establishing tunnels behind NAT (NAT traversal). Enabled by default.
Internet access - When “Online” is show, device has internet connectivity.
Force 4789 UDP port - When enabled, Firewall-Router devices establishing tunnels to this device will use destination port 4789.
Monitor internet - Enables internet monitoring. When disabled, device assumes interface is always online.
VLAN sub-interface - Add VLAN sub-interface to the selected parent interface. Can be used with WAN, LAN or in pure TRUNK (untagged) mode. 

Note
By default each WAN interface has gateway monitoring enabled. Its purpose is to monitor and signal to the user if WAN interface is down.
 Gateway monitoring is part of automatic failover functionality, allowing failover between multiple WAN interfaces.
 Automatic failover works out-of-the-box and automatically when there are multiple WAN interfaces present.
 Interface which has lower metric, will have higher priority. Eg. bottom interface from the screenshot has higher priority.

Note
CMS-GUI enables NAT Traversal by default, allowing devices to establish tunnels behind NAT, when there is a private IP on Firewall-Router WAN.
 NAT Traversal setting is automatically adjusted if it detects public or private IP on WAN, so there is no need to disable it in most cases.

The following settings are available for LAN interfaces:
Assigned - When set to “Yes” interface can be configured. Select “No” to not use the interface with CMS-GUI.
Type - Marks interface as LAN.
IP Address - Shows IP address of LAN interface. IP set here is used as gateway for LAN clients.
MTU - Set Maximum transmission unit (MTU). Default 1500 for all interfaces.
Type - Marks interface as WAN or LAN. CMS-GUI requires at least one WAN and LAN for operation. Multiple WAN and LAN interfaces are supported.
DHCP/Static - Used with WAN interfaces, WAN can be configured with static IP or set to receive an IP via DHCP.
MAC - Displays MAC address of the interface.
Routing - LAN interfaces support OSPF and BGP. By default OSPF is enabled on LAN so interconnected sites can communicate out-of-the-box.
VLAN sub-interfaces - Add VLAN sub-interface to the selected parent interface. Can be used with WAN, LAN or in pure TRUNK (untagged) mode.


DHCP Server
CMS-GUI offers DHCP server support for LAN interfaces. When setting up a LAN interface,
 users will have the option to create a DHCP server, and it is also possible to configure the DHCP server through the interface settings.
CMS-GUI’s DHCP server provides a range of supported features, enhancing your network management capabilities:
IP Range Configuration: You can define the specific range of IP addresses that the DHCP server will allocate to LAN clients.
 This allows you to control the IP address allocation within your network.
DNS Server Configuration for LAN Clients: CMS-GUI allows you to configure DNS server settings for LAN clients.
 This ensures that your clients can resolve domain names and access resources on the internet effectively.
Default and Maximum DHCP Lease Time: You have the flexibility to set both the default and maximum DHCP lease times.
 The default lease time determines how long a client holds an IP address by default, while the maximum lease time can limit the maximum duration of a lease.
Static IP Assignments: CMS-GUI enables you to assign static IP addresses to specific devices on your LAN.
 This is useful for ensuring that critical devices always receive the same IP address, simplifying network management.
Support for DHCP Options: The DHCP server in CMS-GUI supports various DHCP options, including options like 42 and 66, among others.
 These options allow you to customize and optimize DHCP behavior to suit your network requirements.
By leveraging these features, you can tailor your DHCP server settings within CMS-GUI to meet the unique demands of your network, whether it’s for IP management, DNS configuration, or specific DHCP options. 

LAN Bridge
Two or more network interfaces can be bridged when assigned the same IP and CIDR. Clients connected to both or more interfaces will assign or receive an IP from the same range.




If you are not the owner of an account or have been invited to an account,
 you can create a new account by clicking the create account link in the login page.
 Before creating an account please note that there is typically one account per company.
 If you are offering services to customers, you will be able to add each one of your customers as an organization in your account.
 Please refer to Account and Organizations in our documentation to learn more.
 
 
To create owner account, you need to fill following details:
1.  Company Name
2. Business Email
3. First Name
4. Last Name
5. Job Title
6. Phone Number
7. Password

There will be another option in the login page i.e. "Forgot Password"
Which will allow user to reset password by using OTP (send the OTP to the regsieterd email-id).


Add "Audit Trail" in main-menu, create the corresponding table and the page with filter/group/option 
along with a data-grid, update the db script to create the table, Owner can see the Adit-Trials log of
 all and Manager can see the log of all the assigned orgnization and groups, Viewer can see the log of himself only.
  Make sure, no one can delete the records, 
the record will be deleted based on the retention period mentioned in the api/resource/config file 'audit_log_retention_in_days'

